# Discover Corralejo — Approved Walking Routes

All 17 stop-to-stop route geometries were reviewed against the supplied route references and approved on 15 July 2026.

- `data/routes.json` uses `"status": "approved"` for every connection.
- Every file in `data/routes/` also uses `"status": "approved"`.
- The in-app map displays the saved GeoJSON path offline and online.
- Google Maps remains available as an optional live-navigation fallback.

Future route changes should be reviewed before replacing an approved GeoJSON file.
